<p id="notice"><%= notice %></p>

<p>
  <strong>Type:</strong>
  <%= @event.event_type %>
</p>

<p>
  <strong>Gestor:</strong>
  <%= @event.user_id %>
</p>

<p>
  <strong>Private:</strong>
  <%= @event.private %>
</p>

<p>
  <strong>Start:</strong>
  <%= @event.date_start %>
</p>

<p>
  <strong>End:</strong>
  <%= @event.date_end %>
</p>

<%= link_to 'Matches', event_matches_path(@event) %>

<%= link_to 'Edit', edit_event_path(@event) if current_user && current_user.role != 1 %> |
<%= link_to 'Back', events_path %>
